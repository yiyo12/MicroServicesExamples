server:
  port: 8090

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/yiyo12/micro-configs
          clone-one-start: true
          skipSslValidation: true

